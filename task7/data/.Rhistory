for (i in 2:length(subjects)) {
subID <- toString(subjects[i])
subID_names <- append(subID)
}
subID_names <- NULL
for (i in 1:length(subjects)) {
subID <- toString(subjects[i])
subID_names <- append(subID, subID)
}
subID_names <- NULL
for (i in 1:length(subjects)) {
subID <- toString(subjects[i])
subID_names <- append(subID, subID)
}
taskName <- "task7"  # just use this for now to save hassle of always having to enter 'task7'
path <- sprintf("/Users/cdlab_admin/Documents/wtw_work/analysis/%s/data", taskName) # establish path to data
setwd(path) # set this path as the working directory
# Assign subjects results to data frame
subjects <- c(160:199) # subID's for this experiment are '160' to '199'
# Initialize vectors for the different data parameters
blockNum <- NULL
trialNum <- NULL
initialTime <-NULL
itiKeyPresses <- NULL
designatedWait <- NULL
rwdOnsetTime <- NULL
latency <- NULL
outcomeTime <- NULL
payoff <- NULL
totalEarned <- NULL
subID_names <- NULL
for (i in 1:length(subjects)) {
subID <- toString(subjects[i])
subID_names <- append(subID, subID)
}
length(subjects)
taskName <- "task7"  # just use this for now to save hassle of always having to enter 'task7'
path <- sprintf("/Users/cdlab_admin/Documents/wtw_work/analysis/%s/data", taskName) # establish path to data
setwd(path) # set this path as the working directory
# Assign subjects results to data frame
subjects <- c(160:199) # subID's for this experiment are '160' to '199'
# Initialize vectors for the different data parameters
blockNum <- NULL
trialNum <- NULL
initialTime <-NULL
itiKeyPresses <- NULL
designatedWait <- NULL
rwdOnsetTime <- NULL
latency <- NULL
outcomeTime <- NULL
payoff <- NULL
totalEarned <- NULL
subID_names <- toString(subjects[1])
for (i in 1:length(subjects)) {
subID <- toString(subjects[i])
subID_names <- append(subID, subID)
}
taskName <- "task7"  # just use this for now to save hassle of always having to enter 'task7'
path <- sprintf("/Users/cdlab_admin/Documents/wtw_work/analysis/%s/data", taskName) # establish path to data
setwd(path) # set this path as the working directory
# Assign subjects results to data frame
subjects <- c(160:199) # subID's for this experiment are '160' to '199'
# Initialize vectors for the different data parameters
blockNum <- NULL
trialNum <- NULL
initialTime <-NULL
itiKeyPresses <- NULL
designatedWait <- NULL
rwdOnsetTime <- NULL
latency <- NULL
outcomeTime <- NULL
payoff <- NULL
totalEarned <- NULL
# Initialize vector of subject ID names
subID_names <- c(toString(subjects[1]))
for (i in 1:length(subjects)) {
subID <- toString(subjects[i])
subID_names <- append(subID, subID)
}
# Initialize vector of subject ID names
subID_names <- c(toString(subjects[1]))
for (i in 1:length(subjects)) {
subID <- toString(subjects[i])
subID_names <- append(subID_names, subID)
}
subID_names <- NULL
for (i in 1:length(subjects)) {
subID <- toString(subjects[i])
subID_names <- append(subID_names, subID)
}
for (i in length(subjects)) {
subID <- toString(subjects[i])
filename <- sprintf("wtw-work-7_%s_1.mat", subID)
sub_data <- read.mat(filename)
sub_data <- sub_data[2]
blockNum <- append(blockNum, sub_data$trialData$blockNum)
}
View(blockNum)
subID <- toString(subjects[1])
filename <- sprintf("wtw-work-7_%s_1.mat", subID)
sub_data <- read.mat(filename)
sub_data <- sub_data[2]
blockNum <- append(blockNum, sub_data$trialData$blockNum)
blockNum <- NULL
subID <- toString(subjects[1])
filename <- sprintf("wtw-work-7_%s_1.mat", subID)
sub_data <- read.mat(filename)
sub_data <- sub_data[2]
blockNum <- append(blockNum, sub_data$trialData$blockNum)
View(blockNum)
taskName <- "task7"  # just use this for now to save hassle of always having to enter 'task7'
path <- sprintf("/Users/cdlab_admin/Documents/wtw_work/analysis/%s/data", taskName) # establish path to data
setwd(path) # set this path as the working directory
# Assign subjects results to data frame
subjects <- c(160:199) # subID's for this experiment are '160' to '199'
# Initialize vectors for the different data parameters
blockNum <- NULL
trialNum <- NULL
initialTime <-NULL
itiKeyPresses <- NULL
designatedWait <- NULL
rwdOnsetTime <- NULL
latency <- NULL
outcomeTime <- NULL
payoff <- NULL
totalEarned <- NULL
# Initialize vector of subject ID names
subID_names <- NULL
for (i in 1:length(subjects)) {
subID <- toString(subjects[i])
subID_names <- append(subID_names, subID)
}
for (i in length(subjects)) {
subID <- toString(subjects[i])
filename <- sprintf("wtw-work-7_%s_1.mat", subID)
sub_data <- read.mat(filename)
sub_data <- sub_data[2]
blockNum[,i] <- sub_data$trialData$blockNum
}
taskName <- "task7"  # just use this for now to save hassle of always having to enter 'task7'
path <- sprintf("/Users/cdlab_admin/Documents/wtw_work/analysis/%s/data", taskName) # establish path to data
setwd(path) # set this path as the working directory
# Assign subjects results to data frame
subjects <- c(160:199) # subID's for this experiment are '160' to '199'
# Initialize vectors for the different data parameters
blockNum <- matrix(ncol = length(subjects))
trialNum <- NULL
initialTime <-NULL
itiKeyPresses <- NULL
designatedWait <- NULL
rwdOnsetTime <- NULL
latency <- NULL
outcomeTime <- NULL
payoff <- NULL
totalEarned <- NULL
# Initialize vector of subject ID names
subID_names <- NULL
for (i in 1:length(subjects)) {
subID <- toString(subjects[i])
subID_names <- append(subID_names, subID)
}
# Fill in 'blockNum'
for (i in length(subjects)) {
subID <- toString(subjects[i])
filename <- sprintf("wtw-work-7_%s_1.mat", subID)
sub_data <- read.mat(filename)
sub_data <- sub_data[2]
blockNum[,i] <- sub_data$trialData$blockNum
}
blockNum <- matrix(nrow = n.rows, ncol = length(subjects))
blockNum <- matrix()
for (i in length(subjects)) {
subID <- toString(subjects[i])
filename <- sprintf("wtw-work-7_%s_1.mat", subID)
sub_data <- read.mat(filename)
sub_data <- sub_data[2]
blockNum[,i] <- sub_data$trialData$blockNum
}
View(sub_data$trialData$blockNum)
blockNum <- matrix()
for (i in length(subjects)) {
subID <- toString(subjects[i])
filename <- sprintf("wtw-work-7_%s_1.mat", subID)
sub_data <- read.mat(filename)
sub_data <- sub_data[2]
blockNum[,i] <- t(sub_data$trialData$blockNum)
}
blockNum <- matrix(ncol = length(subjects))
# Fill in 'blockNum'
for (i in length(subjects)) {
subID <- toString(subjects[i])
filename <- sprintf("wtw-work-7_%s_1.mat", subID)
sub_data <- read.mat(filename)
sub_data <- sub_data[2]
blockNum_individ = t(sub_data$trialData$blockNum)
blockNum[,i] <- blockNum_individ
}
View(blockNum)
sub_data$trialData$blockNum
a <- sub_data$trialData$blockNum
taskName <- "task7"  # just use this for now to save hassle of always having to enter 'task7'
path <- sprintf("/Users/cdlab_admin/Documents/wtw_work/analysis/%s/data", taskName) # establish path to data
setwd(path) # set this path as the working directory
# Assign subjects results to data frame
subjects <- c(160:199) # subID's for this experiment are '160' to '199'
# Initialize vectors for the different data parameters
blockNum <- matrix(ncol = length(subjects))
trialNum <- NULL
initialTime <-NULL
itiKeyPresses <- NULL
designatedWait <- NULL
rwdOnsetTime <- NULL
latency <- NULL
outcomeTime <- NULL
payoff <- NULL
totalEarned <- NULL
# Initialize vector of subject ID names
subID_names <- NULL
for (i in 1:length(subjects)) {
subID <- toString(subjects[i])
subID_names <- append(subID_names, subID)
}
subID <- toString(subjects[1])
filename <- sprintf("wtw-work-7_%s_1.mat", subID)
sub_data <- read.mat(filename)
sub_data <- sub_data[2]
blockNum_individ = t(sub_data$trialData$blockNum)
blockNum[,i] <- blockNum_individ
sub_data$trialData$blockNum[1]
sub_data$trialData$blockNum[2]
sub_data$trialData$blockNum[450]
taskName <- "task7"  # just use this for now to save hassle of always having to enter 'task7'
path <- sprintf("/Users/cdlab_admin/Documents/wtw_work/analysis/%s/data", taskName) # establish path to data
setwd(path) # set this path as the working directory
# Assign subjects results to data frame
subjects <- c(160:199) # subID's for this experiment are '160' to '199'
# Initialize vectors for the different data parameters
blockNum <- matrix(nrow = 1000, ncol = length(subjects))
trialNum <- NULL
initialTime <-NULL
itiKeyPresses <- NULL
designatedWait <- NULL
rwdOnsetTime <- NULL
latency <- NULL
outcomeTime <- NULL
payoff <- NULL
totalEarned <- NULL
# Initialize vector of subject ID names
subID_names <- NULL
for (i in 1:length(subjects)) {
subID <- toString(subjects[i])
subID_names <- append(subID_names, subID)
}
for (i in length(subjects)) {
subID <- toString(subjects[i])
filename <- sprintf("wtw-work-7_%s_1.mat", subID)
sub_data <- read.mat(filename)
sub_data <- sub_data[2]
blockNum_individ = t(sub_data$trialData$blockNum)
blockNum[,i] <- blockNum_individ
}
for (i in length(subjects)) {
subID <- toString(subjects[i])
filename <- sprintf("wtw-work-7_%s_1.mat", subID)
sub_data <- read.mat(filename)
sub_data <- sub_data[2]
blockNum_individ = t(sub_data$trialData$blockNum)
blockNum[1:length(blockNum_individ),i] <- blockNum_individ
}
View(blockNum)
blockNum <- NULL
trialNum <- NULL
initialTime <-NULL
itiKeyPresses <- NULL
designatedWait <- NULL
rwdOnsetTime <- NULL
latency <- NULL
outcomeTime <- NULL
payoff <- NULL
totalEarned <- NULL
# Initialize vector of subject ID names
subID_names <- NULL
for (i in 1:length(subjects)) {
subID <- toString(subjects[i])
subID_names <- append(subID_names, subID)
}
# Fill in 'blockNum'
for (i in length(subjects)) {
subID <- toString(subjects[i])
filename <- sprintf("wtw-work-7_%s_1.mat", subID)
sub_data <- read.mat(filename)
sub_data <- sub_data[2]
blockNum_individ = t(sub_data$trialData$blockNum)
blockNum <- append(blockNum, blockNum_individ)
}
blockNum <- NULL
subID <- toString(subjects[1])
filename <- sprintf("wtw-work-7_%s_1.mat", subID)
sub_data <- read.mat(filename)
sub_data <- sub_data[2]
blockNum_individ = t(sub_data$trialData$blockNum)
blockNum <- append(blockNum, blockNum_individ)
View(blockNum)
View(blockNum_individ)
blockNum_individ <- sub_data$trialData$blockNum
blockNum_individ <- t(blockNum_individ)
View(blockNum_individ)
subID <- toString(subjects[2])
filename <- sprintf("wtw-work-7_%s_1.mat", subID)
sub_data <- read.mat(filename)
sub_data <- sub_data[2]
blockNum_individ <- sub_data$trialData$blockNum
blockNum_individ <- t(blockNum_individ)
blockNum <- append(blockNum, blockNum_individ)
View(blockNum)
blockNum[50]
blockNum[460]
blockNum[600]
taskName <- "task7"  # just use this for now to save hassle of always having to enter 'task7'
path <- sprintf("/Users/cdlab_admin/Documents/wtw_work/analysis/%s/data", taskName) # establish path to data
setwd(path) # set this path as the working directory
# Assign subjects results to data frame
subjects <- c(160:199) # subID's for this experiment are '160' to '199'
# Initialize vectors for the different data parameters
blockNum <- NULL
trialNum <- NULL
initialTime <-NULL
itiKeyPresses <- NULL
designatedWait <- NULL
rwdOnsetTime <- NULL
latency <- NULL
outcomeTime <- NULL
payoff <- NULL
totalEarned <- NULL
# Initialize vector of subject ID names
subID_names <- NULL
for (i in 1:length(subjects)) {
subID <- toString(subjects[i])
subID_names <- append(subID_names, subID)
}
for (i in length(subjects)) {
subID <- toString(subjects[i])
filename <- sprintf("wtw-work-7_%s_1.mat", subID)
sub_data <- read.mat(filename)
sub_data <- sub_data[2]
blockNum_individ <- sub_data$trialData$blockNum
blockNum_individ <- t(blockNum_individ)
blockNum <- append(blockNum, blockNum_individ)
}
taskName <- "task7"  # just use this for now to save hassle of always having to enter 'task7'
path <- sprintf("/Users/cdlab_admin/Documents/wtw_work/analysis/%s/data", taskName) # establish path to data
setwd(path) # set this path as the working directory
# Assign subjects results to data frame
subjects <- c(160:199) # subID's for this experiment are '160' to '199'
# Initialize vectors for the different data parameters
blockNum <- NULL
trialNum <- NULL
initialTime <-NULL
itiKeyPresses <- NULL
designatedWait <- NULL
rwdOnsetTime <- NULL
latency <- NULL
outcomeTime <- NULL
payoff <- NULL
totalEarned <- NULL
# Initialize vector of subject ID names
subID_names <- NULL
for (i in 1:length(subjects)) {
subID <- toString(subjects[i])
subID_names <- append(subID_names, subID)
}
subID <- toString(subjects[1])
filename <- sprintf("wtw-work-7_%s_1.mat", subID)
sub_data <- read.mat(filename)
sub_data <- sub_data[2]
blockNum_individ <- sub_data$trialData$blockNum
blockNum_individ <- t(blockNum_individ)
blockNum <- append(blockNum, blockNum_individ)
subID <- toString(subjects[2])
filename <- sprintf("wtw-work-7_%s_1.mat", subID)
sub_data <- read.mat(filename)
sub_data <- sub_data[2]
blockNum_individ <- sub_data$trialData$blockNum
blockNum_individ <- t(blockNum_individ)
blockNum <- append(blockNum, blockNum_individ)
subID <- toString(subjects[3])
filename <- sprintf("wtw-work-7_%s_1.mat", subID)
sub_data <- read.mat(filename)
sub_data <- sub_data[2]
blockNum_individ <- sub_data$trialData$blockNum
blockNum_individ <- t(blockNum_individ)
blockNum <- append(blockNum, blockNum_individ)
subID <- toString(subjects[4])
filename <- sprintf("wtw-work-7_%s_1.mat", subID)
sub_data <- read.mat(filename)
sub_data <- sub_data[2]
blockNum_individ <- sub_data$trialData$blockNum
blockNum_individ <- t(blockNum_individ)
blockNum <- append(blockNum, blockNum_individ)
subID <- toString(subjects[5])
filename <- sprintf("wtw-work-7_%s_1.mat", subID)
sub_data <- read.mat(filename)
sub_data <- sub_data[2]
blockNum_individ <- sub_data$trialData$blockNum
blockNum_individ <- t(blockNum_individ)
blockNum <- append(blockNum, blockNum_individ)
subID <- toString(subjects[40])
filename <- sprintf("wtw-work-7_%s_1.mat", subID)
sub_data <- read.mat(filename)
sub_data <- sub_data[2]
blockNum_individ <- sub_data$trialData$blockNum
blockNum_individ <- t(blockNum_individ)
blockNum <- append(blockNum, blockNum_individ)
subID <- toString(subjects[39])
filename <- sprintf("wtw-work-7_%s_1.mat", subID)
sub_data <- read.mat(filename)
sub_data <- sub_data[2]
blockNum_individ <- sub_data$trialData$blockNum
blockNum_individ <- t(blockNum_individ)
blockNum <- append(blockNum, blockNum_individ)
taskName <- "task7"  # just use this for now to save hassle of always having to enter 'task7'
path <- sprintf("/Users/cdlab_admin/Documents/wtw_work/analysis/%s/data", taskName) # establish path to data
setwd(path) # set this path as the working directory
# Assign subjects results to data frame
subjects <- c(160:199) # subID's for this experiment are '160' to '199'
# Initialize vectors for the different data parameters
blockNum <- NULL
trialNum <- NULL
initialTime <-NULL
itiKeyPresses <- NULL
designatedWait <- NULL
rwdOnsetTime <- NULL
latency <- NULL
outcomeTime <- NULL
payoff <- NULL
totalEarned <- NULL
# Initialize vector of subject ID names
subID_names <- NULL
for (i in 1:length(subjects)) {
subID <- toString(subjects[i])
subID_names <- append(subID_names, subID)
}
for (i in length(subjects)) {
subID <- toString(subjects[i])
filename <- sprintf("wtw-work-7_%s_1.mat", subID)
sub_data <- read.mat(filename)
sub_data <- sub_data[2]
blockNum_individ <- sub_data$trialData$blockNum
blockNum_individ <- t(blockNum_individ)
blockNum <- append(blockNum, blockNum_individ)
}
for (i in length(subjects)) {
subID <- toString(subjects[i])
filename <- sprintf("wtw-work-7_%s_1.mat", subID)
sub_data <- read.mat(filename)
sub_data <- sub_data[2]
blockNum_individ <- sub_data$trialData$blockNum
blockNum_individ <- t(blockNum_individ)
blockNum <- append(blockNum, blockNum_individ)
}
blockNum <- NULL
for (i in length(subjects)) {
subID <- toString(subjects[i])
filename <- sprintf("wtw-work-7_%s_1.mat", subID)
sub_data <- read.mat(filename)
sub_data <- sub_data[2]
blockNum_individ <- sub_data$trialData$blockNum
blockNum_individ <- t(blockNum_individ)
blockNum <- append(blockNum, blockNum_individ)
}
taskName <- "task7"  # just use this for now to save hassle of always having to enter 'task7'
path <- sprintf("/Users/cdlab_admin/Documents/wtw_work/analysis/%s/data", taskName) # establish path to data
setwd(path) # set this path as the working directory
# Assign subjects results to data frame
subjects <- c(160:199) # subID's for this experiment are '160' to '199'
# Initialize vectors for the different data parameters
blockNum <- NULL
trialNum <- NULL
initialTime <-NULL
itiKeyPresses <- NULL
designatedWait <- NULL
rwdOnsetTime <- NULL
latency <- NULL
outcomeTime <- NULL
payoff <- NULL
totalEarned <- NULL
# Initialize vector of subject ID names
subID_names <- NULL
for (i in 1:length(subjects)) {
subID <- toString(subjects[i])
subID_names <- append(subID_names, subID)
}
for (i in 1:length(subjects)) {
subID <- toString(subjects[i])
filename <- sprintf("wtw-work-7_%s_1.mat", subID)
sub_data <- read.mat(filename)
sub_data <- sub_data[2]
blockNum_individ <- sub_data$trialData$blockNum
blockNum_individ <- t(blockNum_individ)
blockNum <- append(blockNum, blockNum_individ)
}
blockNum <- NULL
for (i in 1:length(subjects)) {
subID <- toString(subjects[i])
filename <- sprintf("wtw-work-7_%s_1.mat", subID)
sub_data <- read.mat(filename)
sub_data <- sub_data[2]
blockNum_individ <- sub_data$trialData$blockNum
blockNum_individ <- t(blockNum_individ)
blockNum <- append(blockNum, blockNum_individ)
}
View(blockNum)
